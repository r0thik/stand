## Установка инструментов на Kali

```bash
sudo apt install netcat-openbsd -y
sudo apt install hping3 -y
sudo apt install python3-scapy python3-netfilterqueue -y
```
- netcat-openbsd — это современная и функциональная версия netcat, которая подходит для большинства задач (ручные HTTP-запросы, передача файлов, reverse shells).
- hping3 используется для генерации специализированных пакетов (SYN flood, ICMP ping, сканирование портов и т.д.).
- python3-scapy — полная библиотека для создания, отправки и анализа сетевых пакетов.
- python3-netfilterqueue — позволяет перехватывать и изменять пакеты в цепочках Netfilter.

## Генерация трафика (netcat)

### Подготовка веб-сервера на Windows Server

Был запущен HTTP-сервер с помощью встроенного класса .NET `HttpListener` в PowerShell. Сервер слушает порт 8080 на всех интерфейсах и отвечает на любой GET-запрос фиксированной HTML-страницей.

```powershell
$listener = New-Object System.Net.HttpListener
$listener.Prefixes.Add("http://+:8080/")
$listener.Start()
Write-Host "Server started, waiting for requests..."

while ($listener.IsListening) {
    $context = $listener.GetContext()
    $response = $context.Response
    $html = "<html><body><h1>Hello from Windows Server!</h1></body></html>"
    $buffer = [System.Text.Encoding]::UTF8.GetBytes($html)
    $response.ContentLength64 = $buffer.Length
    $response.OutputStream.Write($buffer, 0, $buffer.Length)
    $response.OutputStream.Close()
}
```

- `New-Object System.Net.HttpListener` – создаёт объект HTTP-слушателя.
- `.Prefixes.Add("http://+:8080/")` – добавляет префикс (URL), на котором слушатель будет принимать запросы. `+` означает «все сетевые интерфейсы».
- `.Start()` – запускает прослушивание.
- Цикл `while` – обрабатывает входящие запросы:
  - `GetContext()` – ожидает подключения.
  - Формируется HTML-ответ и преобразуется в байтовый массив.
  - Устанавливается длина содержимого и ответ отправляется через поток вывода.
 
<img width="898" height="506" alt="{C6163DCF-6ECF-4D0A-A6F1-8AEE0720D03A}" src="https://github.com/user-attachments/assets/cfd94243-15b5-41a4-9074-190595931b0e" />

### Отправка HTTP-запроса с Kali Linux с помощью netcat

На атакующей машине была использована утилита `nc` (netcat) для установки TCP-соединения с портом 8080 целевого сервера и передачи HTTP GET-запроса.

```bash
echo -e "GET / HTTP/1.1\nHost: 192.168.211.10\n\n" | nc 192.168.211.10 8080
```

- `echo -e "GET / HTTP/1.1\nHost: 192.168.211.10\n\n"` – генерирует строку с HTTP-запросом:
  - `GET / HTTP/1.1` – метод GET, корневой ресурс `/`, версия протокола 1.1.
  - `Host: 192.168.211.10` – обязательный заголовок Host для HTTP/1.1.
  - Два перевода строки (`\n\n`) – завершение заголовков.
- `| nc 192.168.211.10 8080` – передаёт сгенерированную строку через netcat. `nc` открывает TCP-сокет к указанному IP и порту, отправляет данные, затем выводит полученный ответ.

<img width="669" height="214" alt="{D06EAA52-E80C-4F5C-910A-BEC70CD57C0A}" src="https://github.com/user-attachments/assets/215b4412-3fcb-402a-a635-588a4be68952" />

## Анализ результата

- Строка статуса: `HTTP/1.1 200 OK` – сервер понял запрос и отвечает успешно.
- Заголовки:
   - `Content-Length: 61` – длина тела ответа в байтах (ровно 61 байт для переданной HTML-строки).
   - `Server: Microsoft-HTTPAPI/2.0` – указывает, что сервером является встроенный HTTP-стек Windows (HttpListener).
   - `Date` – дата и время обработки запроса.
- HTML-строка, сформированная скриптом на Windows Server.

## Генерация ICMP-запроса (ping) с помощью scapy

### Первая попытка (неудачная)

Был написан скрипт, формирующий ICMP-пакет только с заголовком (без данных).

```python
#!/usr/bin/env python3
from scapy.all import *

target = "192.168.211.10"
packet = IP(dst=target)/ICMP()
reply = sr1(packet, timeout=2, verbose=True)
if reply:
    print(f"Ответ от {reply[IP].src}: тип ICMP {reply[ICMP].type}")
else:
    print("Нет ответа")
```

- IP(dst=target)/ICMP() – создаёт IP-пакет с протоколом ICMP без поля данных.
- sr1() – отправляет пакет и ожидает ответ.

**Результат:**

<img width="443" height="146" alt="{E3C0A936-1719-4E3C-B84D-8D4F25E8D6E6}" src="https://github.com/user-attachments/assets/2459e62e-a71b-46f1-9a5e-6f26a79f4a49" />

<img width="829" height="452" alt="{51A3DD3B-6221-402A-9CE5-040DE2D20F71}" src="https://github.com/user-attachments/assets/f4b09ee1-ec78-4b2b-a493-7f27186ca9f9" />


**Анализ:**  
- Отправленный пакет имел длину 8 байт (только заголовок ICMP), поля `id` и `seq` равны 0.  
- Windows Server 2022 игнорирует такие запросы как некорректные.  
- Системная утилита `ping` также перестала получать ответы на несколько минут – проявление защитного механизма.

### Вторая попытка (успешная)

Добавлено поле данных (56 байт), как в стандартном ping.

```python
#!/usr/bin/env python3
from scapy.all import *

target = "192.168.211.10"
packet = IP(dst=target)/ICMP()/Raw(load=b"ping"*14)
reply = sr1(packet, timeout=2, verbose=True)
if reply:
    print(f"Ответ от {reply[IP].src}")
```

- Raw(load=b"ping"*14) – добавляет полезную нагрузку из 56 байт (строка "ping" повторена 14 раз).

**Результат:**

<img width="449" height="145" alt="{D196AF38-0A23-4303-BE4C-0049963676E6}" src="https://github.com/user-attachments/assets/1c110ebc-6b34-4c91-a918-e2eea38dea80" />
<img width="825" height="135" alt="{98EC212F-AC03-4C36-B678-984D23FE957E}" src="https://github.com/user-attachments/assets/a71fdbfb-2d30-4091-8287-81ae856aee5e" />

**Анализ:**  
- `Raw(load=b"ping"*14)` – добавляет 56 байт данных (14 раз по строке "ping").  
- Пакет стал идентичен тому, что отправляет системная утилита `ping`.  
- Windows Server обработал запрос и вернул ICMP Echo Reply.

---

## VLAN Hopping

Был создан скрипт `vlan_hopping.py` для отправки кадра Ethernet с двумя тегами VLAN (QinQ) на роутер (192.168.207.1).

```python
#!/usr/bin/env python3
from scapy.all import *

packet = Ether()/Dot1Q(vlan=10)/Dot1Q(vlan=20)/IP(dst="192.168.207.1")/ICMP()
sendp(packet, iface="eth1", verbose=True)
```

**Запуск:**
```bash
sudo python3 vlan_hopping.py
```
<img width="282" height="87" alt="{BD8FB395-AEB1-4B3F-B496-56470CDCCDDE}" src="https://github.com/user-attachments/assets/f741bb88-38a4-437e-8d18-7cdc5ff32881" />

## Мониторинг на роутере

На роутере (Ubuntu) запущен tcpdump на интерфейсе `ens36` (подключение к Kali) для захвата кадров:

```bash
sudo tcpdump -i ens36 -e -n
```

**Вывод:**
<img width="1221" height="263" alt="{0C75D1A6-D13C-44F4-8B7C-67D55B5DB3E1}" src="https://github.com/user-attachments/assets/0a3b8ffc-0ff8-43b7-8823-a65b77964c95" />

```
16:35:30.276046 00:0c:29:84:ee:90 > ff:ff:ff:ff:ff:ff, ethertype ARP (0x0806), length 60: Request who-has 192.168.207.1 tell 192.168.207.10
16:35:30.276046 00:50:56:c0:00:04 > 00:0c:29:84:ee:90, ethertype ARP (0x0806), length 60: Reply 192.168.207.1 is-at 00:50:56:c0:00:04
16:35:30.276079 00:0c:29:ca:61:a9 > 00:0c:29:84:ee:90, ethertype ARP (0x0806), length 42: Reply 192.168.207.1 is-at 00:0c:29:ca:61:a9
16:35:30.299895 56:c0:00:04:00:0c > 00:50:56:c0:00:50, ethertype Unknown (0x2984), length 60: ...
```

Анали результата:
- Первые три пакета – ARP-обмен (запрос от Kali).
- Четвёртый пакет – это отправленный VLAN-кадр.
- tcpdump не распознал ethertype 0x8100 (802.1Q) и показал `ethertype Unknown (0x2984)`. Это значит, что интерфейс `ens36` не поддерживает или не настроен для работы с тегированными VLAN-кадрами. Был передан кадр, но не были интерпретированы теги.

---

